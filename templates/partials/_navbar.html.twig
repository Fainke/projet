<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home_index') }}"><img src="{{ asset('images/logo.jpg')}}" alt="MJN" class="img-fluid"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample07" aria-controls="navbarsExample07" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExample07">
            <ul class="navbar-nav ml-auto">
                {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_modeles_show') }}">Les Modeles</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_modeles_add') }}">Ajout d'un modele</a>
                        {% endif %}

                    </li>


            </ul>
          <ul class="navbar-nav">
              <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_home_propos') }}">A Propos</a>
              </li>
              <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_home_histoire') }}">Histoire</a>
              </li>
                  {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                      <li class="nav-item">
                          <span class="nav-link">{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}</span>
                      </li>
                      <li>
                          <a class="nav-link" href="{{ path('fos_user_security_logout') }}">
                              {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                          </a>
                      </li>
                  {% else %}

                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('fos_user_security_login') }}">Connexion</a>
                      </li>
                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('app_home_add') }}">Inscription</a>
                      </li>
                  {% endif %}

            </ul>
        </div>
    </div>
</nav>